# Denegar acceso directo a todos los archivos de este directorio
# Protege la base de datos SQLite de ser descargada

<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
